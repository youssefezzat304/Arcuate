GEMINI_API_KEY=
# Optional private Stanza service. Exact browser tokenization is used when absent.
LANGUAGE_ANALYZER_URL=http://127.0.0.1:8090
# Optional fast contextual word-explanation model (server-side only).
GEMINI_LOOKUP_MODEL=gemini-3.5-flash-lite
